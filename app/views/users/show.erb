<h3><%= @user.username.capitalize %>'s History</h3>

<ol>
  <% if @user.rounds.any? %>
    <% @user.rounds.each do |round|%>
      <li><%= round.deck.name %> — <%= round.correct_count %> questions answered correctly</li>
    <% end %>
  <% else %>
    <p>No history</p>
  <% end %>
</ol>

